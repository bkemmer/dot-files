# Sourced by zsh unconditionally (interactive or not, login or not) — unlike
# .zshrc, which non-interactive shells (e.g. pi's shellPath=zsh bash tool) skip.
[[ -f "$HOME/.aliases" ]] && source "$HOME/.aliases"
